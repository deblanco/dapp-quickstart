import styled from 'styled-components'

export const SwapWrapper = styled.span`
  width: 50rem;
  padding: 1.5rem;
  border-radius: ${(props) => props.theme.borderRadius};
  background: ${(props) => props.theme.lightGrey};
`
